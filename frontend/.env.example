# База API. По умолчанию используется относительный путь /api (через Vite proxy).
# Можно указать абсолютный адрес бэкенда, например http://localhost:4000/api
VITE_API_BASE=/api

# Адрес бэкенда для dev-прокси Vite (см. vite.config.ts)
VITE_API_PROXY=http://localhost:4000
